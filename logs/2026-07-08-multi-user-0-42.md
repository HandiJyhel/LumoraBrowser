# 2026-07-08 - Multi-utilisateur local - 0.42.0-dev

## Contexte

Apres validation utilisateur `Go`, reprise du prochain point prioritaire sans empiler de fonctions inutiles : poser une vraie base multi-utilisateur locale dans `PulseBrowser.WinUI`.

## Changements

- Version projet passee a `0.42.0-dev` dans `AGENTS.md` et `MainWindow.xaml.cs`.
- Ajout de `ActiveProfileId` dans `PulseConfig`.
- Ajout de la resolution de chemins par profil local dans `PulseProfilePaths`.
- Ajout de `PulseProfileRegistry` pour decouvrir les profils existants et creer des identifiants de profil propres.
- Ajout d'un selecteur de profil dans l'overlay de connexion.
- Ajout de la creation d'un autre profil depuis le selecteur et depuis `Parametres > Profil`.
- Separation des profils locaux sous `%LOCALAPPDATA%\PulseBrowser\profiles\<id>`.
- Conservation du support des emplacements personnalises.
- Redemarrage volontaire lors du changement vers un autre dossier de profil pour eviter un melange de stores en memoire.
- Affichage du dossier du profil actif dans `Parametres > Profil`.

## Verification

- Premiere tentative `build-winui.cmd` bloquee par l'acces NuGet du bac a sable (`NU1301`).
- Relance autorisee avec acces reseau : restore WinUI reussi, build WinUI reussi avec 0 avertissement et 0 erreur.
- Lancement court de `PulseBrowser.WinUI.exe` reussi : fenetre `Pulse Browser 0.42.0-dev`, processus repondant, fermeture du processus de test.
