# 2026-07-09 — Applications web Pulse 0.47.0-dev

- Deuxième demande utilisateur de la session : reprendre la fonction « Créer un raccourci/Installer comme application » de Chrome, en mieux (synergie sessions éphémères, protections actives, confinement de domaine).
- `Models/WebApps.cs` + `WebApps/WebAppStore.cs` : registre local `webapps.pulse`, chiffré DPAPI comme les autres fichiers de navigation, gate mode invité.
- `WebApps/WebAppLaunchArgs.cs`, `WebApps/WebAppUrlPolicy.cs`, `WebApps/IcoWriter.cs` : logique pure testée (parsing `--app=<id>`, confinement de domaine doux, encapsulation PNG→ICO).
- `WebApps/ShellShortcut.cs` : création/suppression de raccourcis `.lnk` via COM `IShellLinkW`/`IPersistFile`, sans dépendance NuGet.
- `WebView2Bootstrap.cs` : configuration process-wide (dossier profil + anti-télémétrie moteur) extraite de `MainWindow`, réutilisée par `App.xaml.cs` pour les fenêtres d'application lancées en processus séparé — garantit le partage des cookies/sessions.
- `PulseAppWindow.xaml(.cs)` : fenêtre minimale (pas d'onglets, pas de barre d'adresse), `PrivacyEngine` local (bloqueur pubs/trackers, anti-télémétrie, HTTPS, CNAME), barre de confinement de domaine douce (jamais de blocage dur, pour ne pas casser les redirections OAuth Google/Microsoft — le problème même corrigé au palier précédent).
- `App.xaml.cs` : détecte `--app=<id>` et lance directement une `PulseAppWindow` sans passer par la fenêtre principale ; retombe proprement sur le navigateur normal si l'id est introuvable (vérifié).
- `MainWindow.WebApps.cs` : installation depuis la page active (dialogue nom + option raccourci Bureau), icône générée depuis le favicon en cache, ajout automatique du domaine aux sites de confiance (synergie 0.46.0-dev), panneau « Applications » (ouvrir, toujours au premier plan, renommer, désinstaller).
- Menu Pulse et palette Ctrl+K : entrées « Applications » et « Installer comme application ».
- Correction incidente : `ShowPanel` ne masquait jamais `WalletPanel` (oubli du palier 0.45.0-dev) — corrigé en même temps que l'ajout de `WebAppsPanel` à la liste de collapse.
- Vérification : `dotnet test` 94/94 verts (80 existants + 14 nouveaux : `WebAppLaunchArgsTests`, `WebAppUrlPolicyTests`, `IcoWriterTests`). `build-winui.cmd` 0 avertissement, 0 erreur. Lancement court normal : fenêtre `Pulse Browser 0.47.0-dev` répondante. Lancement avec `--app=doesnotexist` : retombe correctement sur la fenêtre principale, pas de crash.
- Limite non vérifiée manuellement dans cette session : le parcours complet clic-à-clic (installer une vraie page, ouvrir depuis le raccourci Menu Démarrer réel) n'a pas été testé interactivement — reste à valider par l'utilisateur.

**Version :** `0.47.0-dev`.
