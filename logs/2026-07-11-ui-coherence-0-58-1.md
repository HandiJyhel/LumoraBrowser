# 2026-07-11 - Pulse Browser 0.58.1-dev - Coherence UI des panneaux secondaires

## Objectif

Faire une passe de maturite UI sans ajouter de nouvelle fonction : rendre les
panneaux secondaires plus calmes, plus coherents et plus faciles a scanner.

## Changements

- Ajout de styles XAML partages pour les panneaux secondaires :
  - cartes de panneau ;
  - titres de page ;
  - titres de section ;
  - textes descriptifs courts.
- `Site actuel` :
  - fond de panneau aligne avec les autres surfaces Pulse ;
  - cartes harmonisees ;
  - recommandation de protection mise en avant dans un bloc dedie ;
  - action `Autoriser le flux de connexion` raccourcie et placee comme action principale ;
  - diagnostic connexion et export regroupes dans la meme zone ;
  - titres et textes secondaires uniformises.
- `Parametres` :
  - navigation laterale un peu plus large ;
  - zone de contenu plus large et plus aeree ;
  - titres harmonises ;
  - textes longs raccourcis dans les sections Navigation, Apparence, Accessibilite, Coffre, Profil, Stockage et Confidentialite ;
  - section Confidentialite rendue plus scannable sans modifier les interrupteurs ni les handlers existants.

## Version

- `AGENTS.md` et `MainWindow.xaml.cs` passes a `0.58.1-dev`.

## Verification

- `dotnet test PulseBrowser.Tests\PulseBrowser.Tests.csproj --no-restore` :
  176/176 tests verts.
- Premier `build-winui.cmd` bloque par l'acces NuGet du bac a sable (`NU1301`).
- `build-winui.cmd` relance avec autorisation reseau : reussi, 0 avertissement,
  0 erreur.

## Limite

- Pas de nouvel installateur genere dans cette etape.
