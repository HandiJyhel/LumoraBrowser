# 2026-07-09 — Portefeuille numérique 0.45.0-dev

- Deuxième chantier du `Go` utilisateur (après l'anti-télémétrie 0.44.0-dev).
- `Wallet/VaultPaymentCard.cs` + `Wallet/PaymentCardUtil.cs` (Luhn, réseau, masquage, expiration — logique pure testée).
- `VaultStore` : blob `cards` séparé (AES-256-GCM, même clé), `recovery_cards`, mode DPAPI, `TryDecrypt` générique, API ListCards/UpsertCard/DeleteCard. Rétro-compatible avec les coffres sans cartes.
- `MainWindow.Wallet.cs` + `WalletPanel` : cartes masquées, barrière PIN/mot de passe, ajout/modification (validation Luhn bloquante, pas de champ CVV), suppression, copie 30 s, « Utiliser sur la page active ».
- Détection de page de paiement : script moniteur passif par moteur + `WalletFillBar` ; remplissage sur clic uniquement (numéro, titulaire, expiration combinée ou séparée, événements input/change) ; CVV jamais touché.
- Accès : palette Ctrl+K « Portefeuille », `Paramètres > Coffre`.
- Vérification : `dotnet test` 75/75 verts (14 nouveaux `WalletTests`) ; `build-winui.cmd` 0 avertissement, 0 erreur.
- Note : le palier 0.44.1-dev (icône d'application) a été livré par une session parallèle entre les deux chantiers.
